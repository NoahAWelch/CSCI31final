export default function ButtonInputs({ type = 'button', id, onClick }) {
 // const handleClick = () => {
   // onClick(answer);
  //};
    return <button className="p-2 rounded-lg shadow-lg" type={type} id={id} onClick={onClick}></button>;
  }


 
   
 
   